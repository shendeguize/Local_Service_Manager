name: Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing version tag to publish, for example v0.1.0"
        required: true
        type: string
      publish_pypi:
        description: "Also publish the Python package to PyPI"
        required: true
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.tag || github.ref }}
          fetch-depth: 0
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Select and validate release tag
        id: release
        env:
          INPUT_TAG: ${{ inputs.tag }}
          REF_NAME: ${{ github.ref_name }}
          SHA: ${{ github.sha }}
        run: |
          TAG="${INPUT_TAG:-$REF_NAME}"
          case "$TAG" in
            v[0-9]*.[0-9]*.[0-9]*) ;;
            *) echo "Invalid release tag: $TAG" >&2; exit 1 ;;
          esac
          git fetch origin main --quiet
          TARGET_SHA="$(git rev-parse "$TAG^{commit}")"
          if [ -z "$INPUT_TAG" ]; then
            test "$TARGET_SHA" = "$SHA"
          fi
          git merge-base --is-ancestor "$TARGET_SHA" origin/main
          VERSION="${TAG#v}"
          export VERSION
          python - <<'PY'
          import json
          import os
          import re
          from pathlib import Path

          version = os.environ["VERSION"]
          runtime = re.search(
              r'^__version__\s*=\s*"([^"]+)"$',
              Path("src/localsm/__init__.py").read_text(encoding="utf-8"),
              re.MULTILINE,
          ).group(1)
          package = json.loads(Path("packages/npm/package.json").read_text(encoding="utf-8"))
          if runtime != version or package["version"] != version:
              raise SystemExit(
                  f"version mismatch: tag={version}, runtime={runtime}, npm={package['version']}"
              )
          PY
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
      - run: rm -rf dist && uv build
      - name: Generate checksums
        run: sha256sum dist/* > dist/SHA256SUMS
      - uses: actions/upload-artifact@v7
        with:
          name: release-dist
          path: dist/
          if-no-files-found: error

  publish-github-release:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.tag || github.ref }}
          fetch-depth: 0
      - uses: actions/download-artifact@v8
        with:
          name: release-dist
          path: dist
      - uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ inputs.tag || github.ref_name }}
          files: dist/*
          generate_release_notes: true

  publish-npm:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: npm
      url: https://www.npmjs.com/package/@shendeguize/local-sm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.tag || github.ref }}
          fetch-depth: 0
      - uses: actions/download-artifact@v8
        with:
          name: release-dist
          path: dist
      - name: Bundle Python wheel into npm package
        run: |
          rm -rf packages/npm/wheel
          mkdir -p packages/npm/wheel
          set -- dist/*.whl
          test "$#" -eq 1
          cp "$1" packages/npm/wheel/
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - name: Publish npm wrapper with provenance
        run: |
          VERSION="$(node -p "require('./packages/npm/package.json').version")"
          # Query with a clean config: the setup-node placeholder token would
          # otherwise turn even public registry reads into 401s.
          if NPM_CONFIG_USERCONFIG=/dev/null npm view "@shendeguize/local-sm@${VERSION}" version >/dev/null 2>&1; then
            echo "@shendeguize/local-sm@${VERSION} is already on the registry, skipping publish"
          else
            npm publish ./packages/npm --access public --provenance
          fi

  publish-pypi:
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.publish_pypi == true }}
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: pypi
      url: https://pypi.org/p/local-sm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: release-dist
          path: dist
      - name: Publish to PyPI with trusted publishing
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
